# Cloudflare Pages automatically maps extensionless routes to matching HTML assets
# and redirects /route.html to /route.
# Do not add /route -> /route.html proxy rules here; that creates an infinite loop.
